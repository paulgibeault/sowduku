# external reference doc (lives in the paulgibeault.github.io repo; symlinked locally)
ARCADE_PLATFORM.md

# local render/verification artifact
game.png

# dev tooling only (Playwright for the test tier); the game ships without it
node_modules/
package-lock.json

# the built deploy artifact (npm run build); what dev.sh stages and Pages ships
dist/

# os / editor cruft
.DS_Store
audio/out/

# Local Claude Code tooling.
.claude/
