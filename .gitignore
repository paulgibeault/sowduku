# external reference doc (lives in the paulgibeault.github.io repo; symlinked locally)
ARCADE_PLATFORM.md

# local render/verification artifact
game.png

# dev tooling only (Playwright for the test tier); the game ships without it
node_modules/
package-lock.json

# os / editor cruft
.DS_Store
